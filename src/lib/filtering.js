// src/lib/filtering.js — re-export del deep module testeable (sin DOM).
// Fuente canónica: src/js/modules/filtering.js (Fase 2). No duplicar lógica aquí.
export { filteredItems } from '../js/modules/filtering.js';
